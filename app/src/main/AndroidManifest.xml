<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Put the following two uses permissions statements for remote db connection -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/trident"
        android:label="@string/app_name"
        android:roundIcon="@drawable/login_bg"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cursova"
        tools:targetApi="31">
        <activity
            android:name=".PresentationLayer.Activities.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".PresentationLayer.Activities.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".PresentationLayer.Activities.StartPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PresentationLayer.Activities.MainPageActivity"
            android:exported="false" />
        <activity
            android:name=".PresentationLayer.Activities.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".PresentationLayer.Activities.LoginActivity"
            android:exported="false"></activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.cursova.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>